[package]
name = "app-core-ffi"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0"
description = "C ABI bridge for app-core"

[lib]
name = "barrier_core_ffi"
crate-type = ["cdylib", "staticlib"]

[dependencies]
app-core = { path = "../app-core" }
tokio = { workspace = true }
serde_json = { workspace = true }
